const dias= 70
const qtdmeses= Math.floor (dias /30)
const qtdiasTotal=  qtdmeses*30
const diasPassados= dias-qtdiasTotal
console.log(diasPassados)

const numDivisor=13
const num=39
const equacao= num%numDivisor
equacao===0 ? console.log ("é divisivel")
:console.log("não é divisivel")

